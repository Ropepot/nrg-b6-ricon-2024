package TaskSheet113;

public class Primitives {
    public static void main(String[] args) {
        
        byte zero = 0;
        char h = 'H';
        int three = 3;
        int one = 1;
        float twoPointZero = 2.0f;
        boolean True = true;

        
        String output = h + "" + three + one + one + zero + " w" + zero + "r" + one + "d " + twoPointZero + " " + True;

        System.out.println(output); 
    }
}
